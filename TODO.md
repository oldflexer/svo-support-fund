# TODO: План доработок проекта «Защитим Родину»

## 🔴 Критические ошибки (требуют немедленного исправления)

- [ ] **Включить валидацию форм WTForms во всех эндпоинтах**  
  - [x] `public/donations.py` – уже используется  
  - [x] `public/volunteers.py` – уже используется  
  - [x] `admin/drives.py` – уже используется  
  - [x] `admin/news.py` – уже используется  
  - [ ] `admin/users.py` – форма создаётся, но метод `validate()` не вызывается. Добавить проверку и обработку ошибок.  
  - [ ] Проверить остальные эндпоинты, где используются формы (например, `login` – но там своя логика).  

## 🟠 Важные проблемы безопасности и надёжности

- [ ] **Создать файл `requirements.txt`**  
  На основе реальных импортов (можно использовать `pipreqs`).

- [ ] **Добавить пример файла `.env`**  
  С перечнем необходимых переменных окружения (секретные ключи, JWT, настройки БД, почта и т.д.).

- [ ] **Обновлять статистику при удалении записей**  
  В эндпоинтах DELETE для пожертвований, волонтёров и сборов пересчитывать и обновлять настройки `sum_donation`, `count_volunteers` и т.п.

- [ ] **Добавить возможность удаления пожертвований и волонтёров в админке**  
  Реализовать кнопки удаления в таблицах с подтверждением.

- [ ] **Проверять наличие связанных пожертвований при удалении сбора**  
  Запрещать удаление, если есть пожертвования с `drive_id = id`, либо обнулять их.

- [ ] **Добавить ограничение размеров загружаемых изображений**  
  (Уже частично есть в `utils.py`, но вынести максимальные размеры в конфиг и добавить предварительную проверку на фронтенде).

## 🟡 Улучшения и рефакторинг (бэкенд)

- [ ] **Написать тесты**  
  Покрыть основные эндпоинты (публичные и админские) юнит-тестами (pytest).

## 🔵 Инфраструктура фронтенда (Vite)

- [x] **Рефакторинг JS: модульная структура**  
  Код разбит на модули, созданы точки входа `admin/main.js` и `index/main.js`.

- [x] **Создана общая функция пагинации**  
  `createPaginatedLoader` вынесена в `pagination.js` и используется во всех списках админки.

- [x] **Реализована система уведомлений для администраторов**  
  Polling каждые 30 секунд, toast о новых пожертвованиях и заявках.

- [x] **Множественные изображения для новостей**  
  Добавлена модель `NewsImage`, интерфейс загрузки и удаления.

- [ ] **Унифицировать модуль уведомлений**  
  Сейчас есть два файла `notification.js` (admin и index). Создать единый в `static/js/utils/` и использовать везде.

- [ ] **Удалить неиспользуемые переменные/модули**  
  Например, `volunteers` ref в `admin/volunteers.js` конфликтует с загрузчиком.

- [ ] **Настроить алиасы в Vite**  
  Добавить `@` для папки `static/js`, чтобы упростить импорты (например, `@/api.js`).

- [ ] **Улучшить обработку ошибок в `apiFetch`**  
  Добавить более информативные сообщения и корректный редирект при проблемах с токеном.

- [ ] **Интеграция собранных файлов с Flask**  
  Создать хелпер для подключения скриптов из `manifest.json` в шаблонах. Настроить подключение дев-сервера в режиме разработки.

- [ ] **Оптимизировать сборку для production**  
  Проверить размер бандлов, настроить `manualChunks` при необходимости. Включить сжатие на сервере.

- [ ] **Добавить клиентскую валидацию форм**  
  Простые проверки (обязательные поля, формат email) для улучшения UX до отправки на сервер.

- [ ] **Улучшить модуль пагинации**  
  Убедиться, что `watch` на фильтры не вызывает лишних запросов при инициализации. Добавить возможность сброса страницы при изменении фильтров (уже есть, но проверить).

## 🟢 Новые функции (опционально)

- [ ] **Экспорт данных в Excel/CSV**  
  Кнопки экспорта в таблицах пожертвований, волонтёров, логов.

- [ ] **Интеграция с онлайн-платежами**  
  Подключить ЮKassa или CloudPayments для приёма донатов.

- [ ] **Раздел «Отзывы/благодарности»**  
  Публикация сообщений от подразделений с модерацией.

- [ ] **Подписка на новости по email**  
  Форма подписки в футере и рассылка через админку.

- [ ] **Веб-аналитика**  
  Добавить поля для счётчиков Яндекс.Метрики и Google Analytics в настройки.

- [ ] **История изменений сборов**  
  Логирование изменений полей сборов (можно через audit, но добавить детали).

- [ ] **API для мобильного приложения**  
  Расширить публичные эндпоинты и добавить документацию (Swagger).

- [ ] **Динамическая загрузка модальных окон**  
  Использовать `defineAsyncComponent` для уменьшения начального бандла.

- [ ] **Улучшение доступности (a11y)**  
  Добавить ARIA-метки, управление фокусом в модалках, семантическую разметку.

- [ ] **Постепенный переход на TypeScript**  
  Для долгосрочной поддерживаемости.
