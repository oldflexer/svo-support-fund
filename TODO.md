# TODO: План доработок проекта «Защитим Родину»

## 🔴 Критические ошибки (требуют немедленного исправления)

- [ ] **Исправить синтаксические ошибки в f-строках**  
  В файлах `admin/donations.py` и `admin/volunteers.py` не экранированы кавычки внутри f-строк (например, `{data['status']}`). Заменить на двойные кавычки или экранирование.

- [ ] **Корректная фильтрация на клиенте**  
  В `index.js` метод `fetchNews` передаёт параметр `status` вместо `category`. Исправить на `?category=${newsFilter.value}`.

- [ ] **Пагинация волонтёров**  
  В `admin.js` метод `prevVolunteersPage` ошибочно использует `donations.value.page`. Заменить на `volunteers.value.page`.

- [ ] **Фильтрация аудит-логов**  
  В `admin/audit.py` условие `if user_id:` не позволяет фильтровать по `user_id = 0`. Заменить на `if user_id is not None:`.

- [ ] **Неверное логирование токена в 2FA**  
  В `auth/management.py` после успешной верификации вызывается `log_action(get_jwt_identity(), ...)`, но `get_jwt_identity()` в этом месте ещё не установлен. Заменить на `user.id`.

## 🟠 Важные проблемы безопасности и надёжности

- [ ] **Включить валидацию форм WTForms**  
  Во всех эндпоинтах (публичных и админских) раскомментировать `if not form.validate(): return jsonify({'errors': form.errors}), 400` и настроить правильную передачу данных (`data=request.get_json()`, `meta={'csrf': False}`).

- [ ] **Проверка загружаемых файлов**  
  В `admin/upload.py` добавить проверку MIME-типа и расширения файла (например, разрешить только `image/jpeg`, `image/png`).

- [ ] **Права доступа в эндпоинтах**  
  В `admin/drives.py` для `GET /drives` используется только `@jwt_required()`. Добавить `@role_required('admin', 'moderator')` для единообразия.

- [ ] **Логирование чувствительных данных**  
  В `admin/drives.py` в `update_drive` в лог попадает весь объект `data`. Убрать или заменить на безопасное логирование (только ID и изменённые поля).

- [ ] **Защита от самоудаления/самодеактивации**  
  Уже есть проверка в `toggle_user` и `delete_user`, но стоит убедиться, что она есть везде.

## 🟡 Проблемы с данными и моделями

- [ ] **Унифицировать работу с датами и часовыми поясами**  
  - Везде на сервере использовать `datetime.now(timezone.utc)`.
  - При сериализации всегда добавлять `+00:00` или `Z`.
  - На клиенте использовать единую функцию `formatDate` с `timeZone: 'Europe/Moscow'`.

- [ ] **Несоответствие модели `Drive`**  
  В модели `Drive` нет поля `urgency`, но в старом коде оно использовалось. Добавить или удалить из шаблонов.

- [ ] **Несоответствие модели `NewsArticle`**  
  В модели отсутствуют поля `region` и `tags`, хотя в формах они есть. Добавить в модель или убрать из форм.

- [ ] **Оптимизация запросов**  
  - В `admin/dashboard.py` заменить цикл по дням на один запрос с группировкой по датам.
  - В `admin/stats.py` объединить несколько запросов (например, для донатов использовать один запрос с несколькими агрегациями).

## 🟢 Улучшения и рефакторинг

- [ ] **Пагинация для публичных эндпоинтов**  
  В `public/drives.py` и `public/news.py` добавить пагинацию (параметры `page`, `per_page`), чтобы не возвращать все записи сразу.

- [ ] **Единообразие работы с настройками**  
  Использовать `get_setting` и `update_setting` во всех модулях (сейчас в `public/donations.py` вызывается `update_setting` напрямую, что нормально, но можно унифицировать).

- [ ] **Удалить неиспользуемые импорты**  
  Пройтись по файлам с помощью `autoflake` и удалить лишние импорты (например, `from tkinter import N` в `admin/news.py`).

- [ ] **Добавить индексы в модели**  
  Для полей, по которым часто фильтруют (`status`, `category`, `is_verified`, `created_at`), добавить `db.Index` или указать `index=True`.

- [ ] **Валидация настроек**  
  В `admin/settings.py` добавить проверку допустимых ключей и типов значений.

- [ ] **Исправить дублирование кода**  
  В `admin/users.py` в `update_user` дважды проверяется `if 'email' in data`. Убрать дубликат.

- [ ] **Удалить отладочный вывод**  
  В `admin.js` убрать `console.log(response)` из метода `enableTwoFactor`.

- [ ] **Корректное форматирование дат в шаблоне**  
  В `index.html` для `featuredArticle.published_at` применить функцию `formatDate` (сейчас вставляется ISO-строка).

## ⚪ Документация и окружение

- [ ] **Создать файл `requirements.txt`**  
  На основе реальных импортов (можно использовать `pipreqs`).

- [ ] **Добавить пример файла `.env`**  
  С перечнем необходимых переменных окружения (секретные ключи и т.д.).

- [ ] **Обновить README.md**  
  Добавить информацию о запуске, настройке и особенностях проекта.

- [ ] **Проверить инициализацию БД в `app.py`**  
  Убедиться, что создание таблиц и тестовых пользователей выполняется в правильном порядке (сначала `db.create_all()`, потом проверка пользователей).